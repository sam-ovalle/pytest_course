[tox]
envlist = py38, py39, py310, py311, py312

[testenv]
deps =
  pytest
  pytest-cov
commands =
  pytest --cov=cards --cov=tests --cov-branch --cov-fail-under=100
package = wheel
wheel_build_env = .pkg
